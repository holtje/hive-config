apiVersion: v1
kind: Service
metadata:
  name: {{ include "vacation-calendar.fullname" . }}
  labels:
    {{- include "vacation-calendar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vacation-calendar.selectorLabels" . | nindent 4 }}
